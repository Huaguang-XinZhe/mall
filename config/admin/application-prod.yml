spring:
  datasource:
    url: jdbc:mysql://mysql:3306/mall?useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Shanghai&useSSL=false&allowPublicKeyRetrieval=true
    username: mufengtech
    password: 59sgYlQuiXoE
    druid:
      initial-size: 5
      min-idle: 10
      max-active: 20
      web-stat-filter:
        exclusions: "*.js,*.gif,*.jpg,*.png,*.css,*.ico,/druid/*"
      stat-view-servlet:
        login-username: druid
        login-password: druid
  redis:
    host: redis
    database: 0
    port: 6379
    password:
    timeout: 300ms

minio:
  endpoint: http://minio:9000
  bucketName: mall
  accessKey: aiWedrA7aIIA8kzuB1cP
  secretKey: GmcQbAxQyUqteUeF1XcCvjfdl05KooyQtrXMIgON

aliyun:
  oss:
    endpoint: oss-cn-hangzhou.aliyuncs.com
    accessKeyId: yourAccessKeyId
    accessKeySecret: yourAccessKeySecret
    bucketName: your-bucket-name
    policy:
      expire: 300
    maxSize: 10
    callback: http://your-production-callback-url/aliyun/oss/callback
    dir:
      prefix: mall/images/

logging:
  file:
    path: /var/logs
  level:
    root: info
    com.macro.mall: info
